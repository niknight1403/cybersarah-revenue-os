import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cybersarah.app',
  appName: 'CyberSarah',
  webDir: 'artifacts/dashboard/dist'
};

export default config;
